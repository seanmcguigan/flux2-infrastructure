apiVersion: image.toolkit.fluxcd.io/v1beta1
metadata:
  name: poker
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: poker
  filterTags:
    pattern: '^development-[a-fA-F0-9]+-(?P<ts>[1-9][0-9]*)'
    extract: '$ts'
  policy:
     numerical:
      order: asc